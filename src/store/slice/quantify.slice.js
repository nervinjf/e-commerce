import { createSlice } from '@reduxjs/toolkit';

export const quantifySlice = createSlice({
    name: 'number',
    initialState: 1,
    reducers: {
        
    }
})

export const {  } = quantifySlice.actions;

export default quantifySlice.reducer;
